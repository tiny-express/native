#include "../builtin.h"
#include "../unit_test.h"

TEST(Builtin_System_Device, GetCpuUsage) {
    //ASSERT_TRUE(getCpuUsage() > 0);
}

TEST(Builtin_System_Device, GetRamUsage) {
    //ASSERT_TRUE(getRamUsage() > 0);
}

TEST(Builtin_System_Device, GetDiskAvailable) {
    //ASSERT_TRUE(getDiskAvailable() > 0);
}